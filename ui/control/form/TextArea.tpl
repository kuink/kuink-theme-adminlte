<textarea
	class="form-control {$disabledClass}" style="display:table-cell" 
	id="{$fieldGuid}"
	name="{$fieldName}"
	rows="{$fieldAttrs['rows']}"
	cols="{$fieldAttrs['cols']}"
	{$disabledAttr}>{$field['value']}</textarea>
